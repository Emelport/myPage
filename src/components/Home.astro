<section id="Home" class="pt-24 min-h-screen flex flex-col items-center px-4 text-center bg-gradient-to-b from-gray-950 to-gray-900">
  <span class="victor-font text-green-200 text-4xl sm:text-6xl lg:text-8xl">const</span>
  <span class="victor-font text-blue-400 text-4xl sm:text-6xl lg:text-8xl mb-2">developer</span>
  <span class="victor-font text-orange-300 text-4xl sm:text-6xl lg:text-8xl">
    =
    <span class="victor-font text-red-400">{"{"}</span>
  </span>

  <!-- Texto animado -->
  <span class="mt-8 victor-font text-gray-400 text-base sm:text-xl md:text-2xl flex items-center justify-center flex-wrap">
    profile:
    <span class="text-green-400 ml-1">"</span>
    <span class="text-white writing-wrapper ml-1">
      FullStack Developer<span class="animate-blink cursor-block"></span>
    </span>
    <span class="text-green-400">"</span>,
  </span>

  <!-- Lista de skills -->
  <span class="mt-4 victor-font text-gray-400 text-base sm:text-xl md:text-2xl flex flex-wrap justify-center gap-x-1">
    skills:
    <span class="text-orange-300">[</span>
    <span class="text-blue-300">"Angular",</span>
    <span class="text-blue-300">"Node.js",</span>
    <span class="text-blue-300">"SQL",</span>
    <span class="text-blue-300">".NET",</span>
    <span class="text-blue-300">"Git"</span>
    <span class="text-orange-300">]</span>
  </span>

  <span class="mt-8 victor-font text-red-400 text-4xl sm:text-6xl lg:text-8xl">{"}"}</span>

  <!-- Botones -->
  <div class="flex flex-col sm:flex-row gap-4 mt-10">
    <a href="#contacto">
      <button class="px-6 py-3 w-full sm:w-auto border-2 border-blue-300 bg-blue-400 text-white rounded hover:bg-blue-500 hover:border-blue-400 transition duration-300">
        My Projects
      </button>
    </a>
    <button class="px-6 py-3 w-full sm:w-auto border-2 border-blue-300 text-blue-400 bg-transparent hover:bg-blue-400 hover:text-white transition duration-300 rounded">
      Contact Me
    </button>
  </div>
</section>

<style>
  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .animate-blink {
    animation: blink 1s steps(1, end) infinite;
  }

  @keyframes writing {
    from { max-width: 0; }
    to { max-width: 100%; }
  }

  .writing-wrapper {
    display: flex;
    overflow: hidden;
    white-space: nowrap;
    max-width: 0;
    animation: writing 3s steps(25, end) forwards;
    font-size: inherit;
  }

  .cursor-block {
    display: inline-block;
    width: 2px;
    background-color: white;
    margin-left: 4px;
    vertical-align: baseline;
  }
</style>
